Terminología de Verbos:

    Buscar --> Metodo interno. Llama al repositorio, tira error cuando no encuentra
    Consultar --> Método público. El asociado a Casos de uso, llama a los métodos internos y devuelve Detalle (records)
    Listar --> Método público que devuelve listas, puede ser con containing
