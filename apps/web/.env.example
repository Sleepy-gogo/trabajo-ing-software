# Destino del proxy /api para pnpm dev y pnpm preview (sin /api al final).
API_PROXY_TARGET=http://localhost:4500

# Hosts adicionales del frontend, separados por comas, sin protocolo ni puerto.
# Ejemplo: demo.ngrok-free.app
WEB_ALLOWED_HOSTS=
